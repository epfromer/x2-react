import { fireEvent } from '@testing-library/react'
import React from 'react'
import { renderComp } from '../../setupTests'
import ContactPicker from '../ContactPicker'
// TODO

// test('handleDelete', async () => {
//   const onChange = jest.fn()
//   const contactsToShow = new Map()
//   contactsToShow.set('Watkins, Sherron', true)
//   const { getByText } = renderComp(
//     <ContactPicker contactsToShow={contactsToShow} onChange={onChange} />
//   )
//   const button = getByText(/handleDelete/i)
//   await fireEvent.click(button)
//   expect(onChange).toHaveBeenCalled()
// })

// test('handleClick', async () => {
//   const onChange = jest.fn()
//   const contactsToShow = new Map()
//   contactsToShow.set('Watkins, Sherron', true)
//   const { getByText } = renderComp(
//     <ContactPicker contactsToShow={contactsToShow} onChange={onChange} />
//   )
//   const button = getByText(/handleClick/i)
//   await fireEvent.click(button)
//   expect(onChange).toHaveBeenCalled()
// })
