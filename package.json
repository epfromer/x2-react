{
  "name": "x2-react",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "engines": {
    "node": ">= 14",
    "yarn": ">= 1"
  },
  "scripts": {
    "clean": "lerna clean",
    "build": "lerna run build",
    "postinstall": "cd ./packages/expo && expo-yarn-workspaces postinstall",
    "yarn": "yarn"
  },
  "devDependencies": {
    "lerna": "latest",
    "serve": "^11.3.2",
    "typescript": "^4.0.3"
  }
}