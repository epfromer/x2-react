{
  "name": "x2-react",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "engines": {
    "node": ">= 12",
    "yarn": ">= 1"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna clean",
    "postinstall": "cd ./packages/expo && expo-yarn-workspaces postinstall",
    "yarn": "yarn"
  },
  "devDependencies": {
    "lerna": "latest",
    "serve": "^11.3.2",
    "typescript": "^4.0.3"
  }
}